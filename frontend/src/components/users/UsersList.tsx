import React, {useEffect} from 'react'
import MetaData from '../../layout/MetaData'

const UsersList: React.FC = () => { // Component that renders all of the users from the database onto the dashboard

  return (

    <>
        <MetaData pageTitle = {`List Of Users`} />
    </>


  )
}

export default UsersList