require('dotenv').config();
import mongoose from 'mongoose';

const PAYMENTS_DB_URI = process.env.PAYMENTS_DB_URI || "";

export const connectPaymentsSchema = (): Promise<any> => {
    try {
       return await
    } 
    
    catch(error) {

    }
}