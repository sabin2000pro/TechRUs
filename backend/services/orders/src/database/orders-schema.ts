require('dotenv').config();
import mongoose from 'mongoose';

const ORDERS_DB_URI = process.env.ORDERS_DB_URI;

export const connectOrdersSchema = async (): Promise<any> => {
    try {

    } 
    
    catch(error) {
        
      if(error) {

      }

    }


}